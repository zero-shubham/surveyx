apiVersion: v1
kind: Service
metadata:
  name: orgsvc
spec:
  selector:
    app.kubernetes.io/name: orgsvc
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30000
  type: NodePort