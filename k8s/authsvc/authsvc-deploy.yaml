apiVersion: apps/v1
kind: Deployment
metadata:
  name: authsvc
  namespace: default
  labels:
    app: authsvc
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: authsvc
  template:
    metadata:
      labels:
        app.kubernetes.io/name: authsvc
    spec:
      containers:
        - name: authsvc
          image: ghcr.io/zero-shubham/authsvc:sha-c1b8a70
          imagePullPolicy: Always
          env:
            - name: DATABASE_URL
              value: 'postgresql://postgres:postgres@authsvc-db:30543/datab'
            - name: JWT_SIGN_KEY
              value: 'asdfwefasvdfg123sdfg34asg'
            - name: OLTP_HTTP_ENDPOINT
              value: 'localhost:4318'
          ports:
            - containerPort: 50050
              name: 'grpc-port'
            - containerPort: 50051
              name: 'jwk-port'
          command: ['/main']

        - name: otel-collector
          image: otel/opentelemetry-collector-contrib:latest
          args: ['--config=/etc/otelcol-contrib/config.yaml']
          volumeMounts:
            - name: otel-collector-config
              mountPath: /etc/otelcol-contrib/
          ports:
            - containerPort: 4318
          env:
            - name: OTEL_EXPORTER_OTLP_TRACES_TIMEOUT
              value: '10s'

      initContainers:
        - name: authsvc-migrate
          image: ghcr.io/zero-shubham/authsvc-migrate:sha-f89a0b5
          env:
            - name: DATABASE_URL
              value: 'postgresql://postgres:postgres@authsvc-db:30543/datab?sslmode=disable'
          args:
            ['-path', '/tmp', '-database', '$(DATABASE_URL)', '-verbose', 'up']
        - name: authsvc-seed
          image: ghcr.io/zero-shubham/authsvc:sha-fe27c43
          env:
            - name: DATABASE_URL
              value: 'postgresql://postgres:postgres@authsvc-db:30543/datab?sslmode=disable'
            - name: SUPERADMIN_EMAIL
              value: 'superadmin@surveyx.com'
            - name: SUPERADMIN_PASS
              value: '123456'
          command: ['/seed']

      volumes:
        - name: otel-collector-config
          configMap:
            name: otel-collector-config
