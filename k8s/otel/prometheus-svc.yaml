apiVersion: v1
kind: Service
metadata:
  name: prometheus-exp
spec:
  selector:
    app.kubernetes.io/name: prometheus
  ports:
    - protocol: TCP
      port: 9090
      nodePort: 30082
  type: NodePort